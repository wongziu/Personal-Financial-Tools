# 迭代 P2 改造计划

## 服务

- `target-diagnosis` 分支读取标的、策略、来源、论点和决策记录。
- 生成 Profile、Strategy Lens、Guardrail、Decision Memo 阶段。
- 运行结果持久化到 `research_agent_runs` 和 `research_agent_stages`。

## UI/API

- AI 面板选择 `标的诊断` 时要求选择标的。
- 结果展示策略适配、风险和下一步动作。

## 测试

- 单元测试验证指定标的能生成诊断阶段。
- e2e 验证标的诊断入口渲染。
