# 迭代 P2 多轮澄清

## 第 1 轮：标的诊断是否直接输出买卖

判断：买卖动作依赖账户、仓位、现金、持有状态和策略。

收束：P2 输出动作建议和条件，不直接写交易流水。

## 第 2 轮：是否必须支持所有策略并行

判断：P2 是 MVP，策略数量有限。

收束：读取当前策略版本并生成策略适配摘要；不做复杂并行图执行。

## 第 3 轮：标的详情页是否立刻重构

判断：标的详情页可后续深化；当前重点是研究工作台 AI 闭环。

收束：P2 先在 AI 研究面板输出标的诊断，后续再嵌入标的详情页。
