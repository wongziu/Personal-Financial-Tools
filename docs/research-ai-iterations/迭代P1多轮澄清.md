# 迭代 P1 多轮澄清

## 第 1 轮：策略筛选是否需要实时行情和财报

判断：外部数据会引入 API、授权、稳定性和证据质量问题。

收束：P1 只使用本地数据。缺少外部数据时写入候选卡片的 `missing_evidence`。

## 第 2 轮：策略是否输出“买入”

判断：策略运行是候选筛选，不是交易执行。

收束：P1 的动作限制为 `加入观察`、`补证据`、`建立论点`、`生成决策草案`、`跳过`。

## 第 3 轮：适配分是否要精确量化

判断：本地数据覆盖有限，过度精确会制造假确定性。

收束：适配分只作为排序辅助，用清晰的命中规则和风险标记解释，不作为收益预测。
