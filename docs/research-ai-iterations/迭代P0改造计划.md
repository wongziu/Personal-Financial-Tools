# 迭代 P0 改造计划

## 数据

- 新增 `strategies`、`strategy_versions`、`research_agent_runs`、`research_agent_stages`。
- 更新 Drizzle schema 和 SQLite 初始化。
- 种子数据增加一个散户可读的主动成长策略。

## 服务

- 新增 `src/lib/research-iteration-workflow.ts`。
- 实现 `runResearchIterationWorkflow`，支持 `strategy-run`、`target-diagnosis`、`review-session` 三类触发。
- 实现运行和阶段持久化。

## UI/API

- 新增 `/api/research-iteration-workflow`。
- 扩展 `ResearchAiPanel`，加入“迭代入口”选择和“运行迭代工作流”按钮。
- 研究工作台展示策略库、策略版本、AI 运行记录。

## 测试

- 新增单元测试验证运行持久化。
- e2e 验证三入口展示和运行结果渲染。
